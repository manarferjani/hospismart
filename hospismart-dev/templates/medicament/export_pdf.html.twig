<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Export Médicaments - OASIS</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        @media print {
            body { background: white; padding: 0; }
            .no-print { display: none; }
            table { page-break-inside: avoid; }
            .page-break { page-break-after: always; }
        }
        .header-export {
            border-bottom: 3px solid #0d6efd;
            padding-bottom: 15px;
            margin-bottom: 20px;
        }
        .logo { font-size: 24px; font-weight: bold; color: #0d6efd; }
        .export-footer {
            text-align: center;
            margin-top: 30px;
            border-top: 1px solid #dee2e6;
            padding-top: 15px;
            font-size: 11px;
            color: #6c757d;
        }
    </style>
</head>
<body class="p-4">
    <div class="no-print mb-3">
        <button onclick="window.print()" class="btn btn-primary"><i class="fas fa-print me-2"></i>Imprimer / Exporter PDF</button>
        <a href="{{ path('app_medicament_index') }}" class="btn btn-secondary"><i class="fas fa-arrow-left me-2"></i>Retour</a>
    </div>

    <div class="header-export">
        <div class="row">
            <div class="col-6">
                <div class="logo"><i class="fas fa-hospital-alt"></i> OASIS</div>
                <p class="text-muted small mb-0">Gestion des Stocks de Médicaments</p>
            </div>
            <div class="col-6 text-end">
                <p class="mb-1"><strong>Date d'export :</strong> {{ exportDate|date('d/m/Y H:i') }}</p>
                <p class="mb-0"><strong>Total :</strong> {{ medicaments|length }} médicament(s)</p>
            </div>
        </div>
    </div>

    <h2 class="mb-3">Inventaire Complet des Médicaments</h2>

    <table class="table table-bordered table-striped small">
        <thead class="table-light">
            <tr>
                <th style="width: 5%;">N°</th>
                <th style="width: 25%;">Nom du Médicament</th>
                <th style="width: 10%; text-align: right;">Quantité</th>
                <th style="width: 10%; text-align: right;">Seuil Alerte</th>
                <th style="width: 12%; text-align: right;">Prix Unitaire</th>
                <th style="width: 12%; text-align: right;">Valeur Total</th>
                <th style="width: 15%;">Date Péremption</th>
                <th style="width: 11%;">Statut</th>
            </tr>
        </thead>
        <tbody>
            {% set totalValue = 0 %}
            {% for medicament in medicaments %}
                {% set itemValue = medicament.quantite * medicament.prixUnitaire %}
                {% set totalValue = totalValue + itemValue %}
                <tr>
                    <td>{{ loop.index }}</td>
                    <td><strong>{{ medicament.nom }}</strong></td>
                    <td style="text-align: right;">{{ medicament.quantite }}</td>
                    <td style="text-align: right;">{{ medicament.seuilAlerte }}</td>
                    <td style="text-align: right;">{{ medicament.prixUnitaire|number_format(2, ',', ' ') }} €</td>
                    <td style="text-align: right;">{{ itemValue|number_format(2, ',', ' ') }} €</td>
                    <td>{{ medicament.datePeremption|date('d/m/Y') }}</td>
                    <td>
                        {% if medicament.quantite <= medicament.seuilAlerte %}
                            <span style="display: inline-block; background: #ffc107; color: #000; padding: 3px 8px; border-radius: 3px; font-size: 9px; font-weight: bold;">⚠️ ALERTE</span>
                        {% else %}
                            <span style="display: inline-block; background: #28a745; color: #fff; padding: 3px 8px; border-radius: 3px; font-size: 9px; font-weight: bold;">✓ OK</span>
                        {% endif %}
                    </td>
                </tr>
            {% endfor %}
        </tbody>
        <tfoot class="table-light" style="font-weight: bold;">
            <tr>
                <td colspan="5" style="text-align: right; padding: 15px 10px;">VALEUR TOTALE DU STOCK :</td>
                <td style="text-align: right; padding: 15px 10px; border-top: 3px solid #0d6efd; border-bottom: 3px solid #0d6efd;">{{ totalValue|number_format(2, ',', ' ') }} €</td>
                <td colspan="2" style="border-top: 3px solid #0d6efd; border-bottom: 3px solid #0d6efd;"></td>
            </tr>
        </tfoot>
    </table>

    <div class="export-footer">
        <p>Document généré par OASIS | Gestion Intelligente des Stocks Hospitaliers</p>
        <p>© 2026 - Strictly Confidential</p>
    </div>
</body>
</html>
