{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary mt-3">
        <i class="fas fa-save"></i> {{ button_label|default('Enregistrer') }}
    </button>
    <a href="{{ path('app_parametre_vital_index') }}" class="btn btn-secondary mt-3">
        <i class="fas fa-times"></i> Annuler
    </a>
{{ form_end(form) }}
